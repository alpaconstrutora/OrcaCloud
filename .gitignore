# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Environment — nunca versionar arquivos de env com segredos
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scripts de desenvolvimento/diagn�stico (n�o fazem parte do produto)
_dev_scripts/

# Ferramentas de IA / agentes
.temp_ag_kit/
.agents/
.antigravity_rules

.vercel

# Saidas geradas pelo harness de render (regeraveis)
docs/spikes/wall-render/saida-*.png
docs/spikes/digitalizador/segmentos.svg
docs/spikes/digitalizador/eixos.svg
docs/spikes/digitalizador/segmentos.png
docs/spikes/digitalizador/raster-componentes.png

# Prints gerados pelos harnesses de verificacao visual
docs/spikes/**/*.png

# Rascunhos de trabalho: SQL de aplicação pontual, backups e scripts de uma vez
# só. Não é código de produto e não deve virar história do repositório — e, sem
# esta linha, um `git add -A` num deploy futuro varre tudo para produção.
scratch/
