const fs = require('fs'); let c = fs.readFileSync('components/InventoryModule.tsx', 'utf8'); c = c.replace(/, StatusBadge /g, ' '); if (!c.includes('const StatusBadge =')) { c = c.replace('export const InventoryModule: React.FC<Props> = ({ activeOrganizationId }) => {', 'const StatusBadge = ({ status, label }: { status: \\'success\\'|\\'danger\\'|\\'warning\\'|\\'info\\', label: string }) => { const colors = { success: \\'bg-emerald-50 text-emerald-700 border-emerald-200\\', danger: \\'bg-rose-50 text-rose-700 border-rose-200\\', warning: \\'bg-amber-50 text-amber-700 border-amber-200\\', info: \\'bg-blue-50 text-blue-700 border-blue-200\\' }; return <span className={px-2 py-1 rounded-md text-xs font-medium border }>{label}</span>; };\\n\\nexport const InventoryModule: React.FC<Props> = ({ activeOrganizationId }) => {'); } fs.writeFileSync('components/InventoryModule.tsx', c);
